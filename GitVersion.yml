mode: Mainline
next-version: 1.0.1
branches:
  main:
    regex: ^main$
    mode: ContinuousDelivery
    label: ''
    increment: Patch
  feature:
    regex: ^features?[/-]
    mode: ContinuousDelivery
    label: alpha
    increment: Minor
